/bin/
*.test
*.out
.DS_Store
.claude/settings.local.json
# stray binary from `go build ./eval/promptfoo/braindump` (use `go run` instead)
/braindump
# graphify knowledge-graph artifacts (built per-repo by styx/graphify)
graphify-out/
# subagent-driven-development scratch (ledger, briefs, reports, review packages)
.superpowers/
# styx's own working artifacts when dogfooding itself (research briefs, plans, handoffs)
/styx/
